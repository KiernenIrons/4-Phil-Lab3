#pragma once
#include <vector>
#include "Brick.h"

static std::vector<Brick*> m_bricks;

static int clayCounter = 0;
static int legoCounter = 0;
static int concreteCounter = 0;

const int SCREEN_WIDTH = 800;
const int SCREEN_HEIGHT = 600;